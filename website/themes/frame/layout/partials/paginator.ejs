<% if (page.total > 1){ %>
    <div class=paginator>
        <div class="h-line-primary"></div>
        <nav class="pagination">
            <%- paginator({
                // prev_text: "&laquo; Prev",
                // next_text: "Next &raquo;"
                prev_next: false,
                end_size: 1,
                mid_size: 1,
            }) %>
        </nav>
    </div>
<% } %>